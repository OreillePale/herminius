apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
  namespace: argocd
data:
  resource.customizations.ignoreDifferences.external-secrets.io_ExternalSecret: |
    jsonPointers:
      - /status
      - /metadata/annotations/reconcile.external-secrets.io~1force-sync
  kustomize.buildOptions.v5: "--enable-helm"
