apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: argocd-custom-tools
  namespace: argocd
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
