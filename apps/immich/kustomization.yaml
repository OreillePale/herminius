resources:
  - postgres.yaml
  - external-secret.yaml
  - pvc-library.yaml
  - values.yaml

helmCharts:
  - name: immich
    repo: oci://ghcr.io/immich-app/immich-charts
    version: "0.12.0"
    releaseName: immich
    namespace: immich
    valuesFile: values.yaml
