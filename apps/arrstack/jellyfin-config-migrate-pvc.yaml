apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyfin-config-migrate
  namespace: arrstack
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: truenas-root-nobackup
