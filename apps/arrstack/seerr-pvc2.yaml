apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seerr-config-2
  namespace: arrstack
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-root-nobackup
  resources:
    requests:
      storage: 2Gi
