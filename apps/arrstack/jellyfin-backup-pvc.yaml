apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyfin-backup-pvc
  namespace: arrstack
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nas-nfs-subdir
  resources:
    requests:
      storage: 2Gi
