apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
  namespace: arrstack
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-subdir
  resources:
    requests:
      storage: 1.2Ti
