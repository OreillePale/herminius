apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nativecloudpg
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/OreillePale/herminius
    targetRevision: main
    path: apps/nativecloudpg

  destination:
    server: https://kubernetes.default.svc
    namespace: nativecloudpg

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
