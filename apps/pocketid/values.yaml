controllers:
  pocketid:
    containers:
      app:
        image:
          repository: ghcr.io/pocket-id/pocket-id
          tag: v2.3.0
        env:
          APP_URL: "https://pocketid.flickers.cc"
          TRUST_PROXY: true
        envFrom:
          - secretRef:
              name: pocketid-secret
