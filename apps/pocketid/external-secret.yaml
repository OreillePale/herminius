apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pocketid-secret
  namespace: pocketid
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-fields-wrapped
    kind: ClusterSecretStore
  target:
    name: pocketid-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: 1b6225fc-de8b-40b1-9050-b40101157f0e
