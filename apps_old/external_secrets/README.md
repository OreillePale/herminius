# External Secrets

## Bitwarden / Vaultwarden

### Renovate
Since we're using vaultwarden, the version of the bitwarden cli container in `values.yaml` need to be on par with the version of vaultwarden, thus the ignorePath command in `renovate.json`
